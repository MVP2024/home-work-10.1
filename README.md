# Проект банковских операций клиента

## Описание проекта

_Данный проект представляет собой скрипт на Python, который реализует функции для обработки и маскирования данных, таких
как номера карт и счетов.
Он также включает функции для фильтрации и сортировки списка данных, содержащих информацию о различных элементах._

### Основные цели проекта

- Фильтрация данных по ключу `state`.
- Сортировка данных по ключу `date`.
- Маскировка номеров счетов и карт.
- Генерация и фильтрация транзакций по валюте.
- Генерация описаний транзакций.
- Генерация номеров карт.
- Загрузка данных из json-файла.
- Конвертация валюты в указанную валюту.


***Установка***

1. Установите [Python](https://www.python.org/downloads/) (рекомендуется версия 3.6 и выше).
2. Клонируйте репозиторий на локальный компьютер:

```
   git clone git@github.com:ваш_пользователь/название_репозитория.git
```
3. Установите необходимые зависимости:
```
pip install -r requirements.txt`
```

4. Создайте файл .env в корневом каталоге проекта и добавьте ваш API ключ:
```
EXCHANGE_API_KEY=ваш_ключ_api
```

5. Перейдите в директорию проекта.

#### Использование

***Импорт функций***
```
    from <your_module_name> import <имя_функции>, <имя_функции>...
```
```Замените <your_module_name> на имя вашего модуля, <имя_функции> на имя соответсвующей функции```


***Функция***
`get_mask_card_number(card_number: Union[str]) -> Union[str]`

**Описание:**
_Принимает на вход номер карты и возвращает его маскированный вид. Номер отображается в формате_ XXXX XX** **** XXXX, 
_где видны первые 6 и последние 4 цифры._

*Пример:*
```
    masked_card = get_mask_card_number("7000792289606361")
    print(masked_card)  # Вывод: "7000 79** **** 6361"
```

***Функция***
`get_mask_account(account_number: Union[str]) -> Union[str]`

**Описание:**
_Принимает на вход номер счета и возвращает его маскированный вид. Номер отображается в формате_ **XXXX, _где видны только
последние 4 цифры._

*Пример:*
```
    masked_account = get_mask_account("73654108430135874305")
    print(masked_account)  # Вывод: "**4305"
```

***Функция***
`mask_account_card(types_requisites: Union[str]) -> Union[str]`

**Описание:**
_Принимает на вход тип и номер карты или счета и возвращает их тип и маскированный номер._

*Пример:*
```
    masked_info = mask_account_card("Visa Platinum 7000792289606361")
    print(masked_info)  # Вывод: "Visa Platinum 7000 79** **** 6361"
```

***Функция***
`get_date(data_full: Union[str]) -> Union[str]`

**Описание:**
_Принимает на вход строку с датой в формате_ "YYYY-MM-DDTHH:MM:SS" _и возвращает строку с датой в формате_ "ДД.ММ.ГГГГ".

*Пример:*
```
    formatted_date = get_date("2024-03-11T02:26:18.671407")
    print(formatted_date)  # Вывод: "11.03.2024"
```

***Функция***
`sort_by_date(data: List[Dict[str, Any]], reverse: bool = True) -> List[Dict[str, Any]]`

**Описание:**
_Принимает список словарей и необязательный параметр для задания порядка сортировки. Возвращает новый список словарей,
отсортированный по дате._

*Пример:*
sorted_data = sort_by_date(data)
### Вывод:
```
    [{'id': 41428829, 'state': 'EXECUTED', 'date': '2019-07-03T18:35:29.512364'}, 
    {'id': 939719570, 'state': 'EXECUTED', 'date': '2018-06-30T02:08:58.425572'}]
```

***Функция***
`filter_by_state(data: List[Dict[str, Any]], state: str = "EXECUTED") -> Union[str, List[Dict[str, Any]]]`

**Описание:**
_Принимает список словарей и фильтрует его по состоянию state._

*Пример:*
filtered_data = filter_by_state(data, "EXECUTED")

### Вывод:
```
    [{'id': 41428829, 'state': 'EXECUTED', 'date': '2019-07-03T18:35:29.512364'}, 
    {'id': 939719570, 'state': 'EXECUTED', 'date': '2018-06-30T02:08:58.425572'}]
```

***Функция***
`filter_by_currency(transactions: List[Dict[str, Any]], currency: str) -> Iterator[Dict[str, Any]]`

**Описание:**
_Функция возвращает итератор, который поочередно выдает транзакции, где валюта операции соответствует заданной._

*Пример:*
filtered_transactions = list(filter_by_currency(transactions, "USD"))

### Вывод: 
```
    {'id': 939719570, 'state': 'EXECUTED', 'date': '2018-06-30T02:08:58.425572', 'operationAmount': 
    {'amount': '9824.07', 'currency': {'name': 'USD', 'code': 'USD'}}, 
    'description': 'Перевод организации', 'from': 'Счет 75106830613657916952', 'to': 'Счет 11776614605963066702'}
```

***Функция***
`transaction_descriptions(transactions: List[Dict[str, Any]]) -> Iterator[str]`

**Описание:**
_Генератор, который принимает список транзакций и возвращает описание каждой операции._

*Пример:*
descriptions = list(transaction_descriptions(transactions))
```
    Перевод организации
    Перевод со счета на счет
```

***Функция***
`card_number_generator(start: int, end: int)`

**Описание:**
_Генератор для создания номеров банковских карт в формате XXXX XXXX XXXX XXXX, где X - цифра._

*Пример:*
generated_numbers = list(card_number_generator(1000, 1003))
```
    0000 0000 0000 1000 
    0000 0000 0000 1001
    0000 0000 0000 1002
    0000 0000 0000 1003
```

***Функция - декоратор***
`def log(filename: Optional[str] = None, time_delay: float = 0) -> Callable:`

**Описание**
_Логируемая информация:
Декоратор записывает следующую информацию:_

1. Время начала выполнения функции.
2. Аргументы, с которыми была вызвана функция.
3. Время выполнения функции.
4. Возвращаемое значение функции или сообщение об ошибке в случае исключения.
_Декоратор для логирования вызовов функций с задержкой
    Аргументы:
    filename (Optional[str]): Имя файла для записи логов. Если None, выводит логи в консоль.
    time_delay (float): Задержка в секундах перед выполнением функции. По-умолчанию 0_

*Пример*
@log("mylog.txt")
def get_mask_account(account_number: Union[str]) -> Union[str, ValueError]:
    """Функция принимает на вход номер счёта и возвращает замаскированный счёт"""

**вызов декоратора**
`get_mask_account(7854121223455678)`

### Запись в log.txt:
```
get_mask_card_number ok. Возвращаемое значение 7854 12** **** 5678. Время выполнения: 0:00:00.000140
Начало get_mask_card_number в 2025-01-07T19:50:28.484311  с аргументами: ('78541212234',), {}
```

***Функция***
`convert_to_rub(transaction: Dict[str, Any]) -> float`

_Конвертирует сумму из указанной валюты в рубли (RUB)._

- **Параметры**:
  - `transaction`: Словарь, представляющий транзакцию, который должен содержать поле `operationAmount` с полями:
    - `amount` (число в виде строки)
    - `currency` (словарь с полем `code`, указывающим код валюты).

- **Исключения**:
  - `ValueError`: 
    - Если транзакция не содержит поле `operationAmount`.
    - Если API не возвращает курс для RUB.
    - Если не удается получить обменный курс.

- **Возвращаемое значение**: 
  - Конвертированная сумма в рублях (RUB).

- **Пример использования**:
  ```
  transaction = {
      "operationAmount": {
          "amount": "1000",
          "currency": {"name": "USD", "code": "USD"}
      }
  }
  rub_amount = convert_to_rub(transaction)
  print(rub_amount)  # Предположим, что курс USD к RUB равен 75.0, результат будет 75000.0 
  ```


***Функция***
`load_transactions(file_path: str) -> List[Dict[str, Any]]`

Загружает данные о транзакциях из JSON-файла.

- **Параметры**:
  - `file_path`: Путь до JSON-файла.

- **Возвращаемое значение**: 
  - Список словарей с данными о транзакциях или пустой список, если файл не найден или данные некорректны.

- **Пример использования**:
  ```
  transactions = load_transactions('path/to/your/transactions.json')
  print(transactions)  # Выводит список транзакций или пустой список
  ```

***Функция***
`def read_financial_operations_from_csv(file_path: str) -> List[Dict[str, str]]`

Загружает данные о транзакциях из csv-файла.

- **Параметры**:
- `file_path (str)`: Путь к файлу csv.

- **Возвращаемое значение**:
  - List[Dict[str, str]]: Список словарей с транзакциями и исключения:
  - FileReadError: Если файл не может быть прочитан.
  - EmptyFileError: Если файл пуст или не содержит данных.
  - InvalidFileFormatError: Если файл не является форматом CSV.
  - FileNotFoundError: Если файл не существует.

- **Пример использования**:
```
    transactions_csv =  read_financial_operations_from_csv("path/to/your/file.csv")
    transaction_csv(file_path: str) -> List[Dict[str, str]]
```

***Функция***
`read_financial_operations_from_excel(file_path: str) -> list[dict[Hashable, Any]]`

Загружает данные о транзакциях из xlsx-файла.

- **Параметры**:
- `file_path (str)`: Путь к файлу csv.

- **Возвращаемое значение**:
  - list[dict[Hashable, Any]]: Список словарей с транзакциями и исключения:
  - FileReadError: Если файл не может быть прочитан.
  - EmptyFileError: Если файл пуст или не содержит данных.
  - InvalidFileFormatError: Если файл не является форматом Excel.
  - FileNotFoundError: Если файл не существует.

- **Пример использования**:
```
    transactions = read_financial_operations_from_excel("path/to/your/file.xlsx")
    transaction_excel(file_path: str) -> list[dict[Hashable, Any]]
```
    

***Функция***
`filter_bank_operations(transactions: List[Dict[str, Any]], search_str: str) -> List[Dict[str, Any]]:`

Предназначена для фильтрации списка банковских операций по заданной строке поиска. 

- **Параметры**:
- `transactions : List[Dict[str, Any]] `
Список транзакций, где каждая транзакция представлена в виде словаря.

- **Возвращаемое значение**:
- `search_str : str`
- Строка поиска, по которой будет производиться фильтрация. Возвращает:
- Возвращает:
- `List[Dict[str, Any]]`


***Функция***
`count_operations_by_category`

Подсчитывает количество транзакций по категориям на основе описания транзакций.

- **Параметры**:
- `ransactions (List[Dict[str, Any]]):` Список транзакций, где каждая транзакция представлена как словарь.
- `categories (List[str]):` Список категорий, по которым нужно подсчитать количество транзакций.

- **Возвращаемое значение**:
- `Dict[str, int]:` Словарь, где ключами являются категории, а значениями — количество транзакций для каждой категории.


***Функция***
`def main() -> None:`

**Назначение:**
`В ней интегрированы функции для работы с банковскими транзакциями, которая управляет процессом загрузки, фильтрации и отображения транзакций.`

    - Настройка логгера: Инициализирует логгер для записи действий и ошибок, происходящих в процессе выполнения программы.

    - Приветствие пользователя: Выводит приветственное сообщение о запуске программы.

**Выбор источника данных:**

    - Предлагает пользователю выбрать источник данных для загрузки транзакций (JSON, CSV, XLSX).

    - Загружает транзакции из выбранного файла и записывает информацию о процессе в лог.

    - Проверка наличия транзакций: Если загруженные транзакции отсутствуют, выводит соответствующее сообщение и завершает выполнение программы.

**Фильтрация по статусу:**

    - Запрашивает у пользователя статус для фильтрации транзакций (EXECUTED, CANCELED, PENDING) или возможность выхода из программы.

    - Проверяет корректность введенного статуса и выполняет фильтрацию транзакций.

**Сортировка транзакций:**

    - Предлагает пользователю выбрать, нужно ли сортировать транзакции по дате (по возрастанию или убыванию).

    - Выполняет сортировку в зависимости от выбора пользователя.

**Фильтрация по валюте:** Позволяет пользователю выбрать, нужно ли выводить только рублевые транзакции.

**Фильтрация по описанию:** Предлагает пользователю возможность отфильтровать транзакции по определенному слову в описании.

**Вывод информации:** После всех фильтраций программа выводит количество операций в выборке и другую соответствующую информацию о транзакциях.

**Логирование действий:** Записывает информацию о действиях пользователя и результатах обработки в лог.

**Завершение работы:** Если пользователь решает выйти из программы, выводит соответствующее сообщение и завершает выполнение.

- **Пример использования**:
```
    Всего банковских операций в выборке: {'Перевод с карты на карту': 20}
    08.07.2021: Перевод с карты на карту
    Visa 0773 09 ** **** 2450 -> Ошибка: номер указан без типа карты/счёта.
    Сумма: 23182 RUB
```

##### Тестирование

1. Для работы функции и тестов требуется установленный пакет pytest.
2. Установите необходимые зависимости.
3. Установить его можно с помощью:

``` 
pip install pytest
```

4. Для запуска тестов используйте следующую команду:

**В модулях с тестами имеется конструкция:** 
```
if __name__ == "__main__": pytest.main()`
```

```Можно запустить все тесты, фикстуры в каждом модуле отдельно```

  Для запуска из терминала:
**Или например для функций из модуля `processing.py`:**  
  ```
  pytest tests/test_processing.py
  ```

5. Для запуска всех тестов используйте команду:

```
pytest
```

6. Для запуска тестов с оценкой покрытия используйте команду:
```При активированном виртуальном окружении.```
``` 
pytest --cov
 ``` 

 ```Через poetry.```
```
poetry run pytest --cov
``` 

 ```Чтобы сгенерировать отчет о покрытии в HTML-формате```
```
pytest --cov=src --cov-report=html
 ```

7. Для проверки какие тесты пропущены введите команду:
```Src это модуль в котором мы ищем пропуски.```
```
pytest --cov=src --cov-report=term-missing
``` 

8. Для проверки какие тесты пропущены в папке tests.
``` Tests это модуль, в котором мы ищем пропуски.```
```
pytest --cov=tests --cov-report=term-missing
```


*Примеры тестов*

- **Тесты для `get_mask_card_number`**:
    - Проверяют различные сценарии, включая корректные и некорректные номера карт.

- **Тесты для `get_mask_account`**:
    - Аналогично проверяют номера счетов.

- **Тесты для `gfilter_by_state`**:
    - Проверяют фильтрацию данных по состоянию.

- **Тесты для `sort_by_date`**:
    - Проверяют сортировку данных по дате и обработку ошибок.

- **Тесты для `mask_account_card`**:
    - Проверяют маскировку с учетом типа.

- **Тесты для `get_date`**:
    - Проверяют корректность форматирования даты.

- **Тесты для `filter_by_currency`**:
    - Проверяют фильтрацию транзакций по валюте.

- **Тесты для `transaction_descriptions`**:
    - Проверяют генерацию описаний транзакций.

- **Тесты для `card_number_generator`**:
    - Проверяют корректность генерации и форматирования номеров карт.

- **Тесты для декоратора `def log`**: 
    - Проверяет в обеспечении его надежности и корректности.

- **Тесты для `convert_to_rub`**:
    - Проверяют корректность конвертации суммы из одной валюты в рубли (RUB).

    - Убеждаются, что функция возвращает ту же сумму, если валюта уже в рублях.

    - Проверяют обработку ошибок, связанных с отсутствием необходимого поля operationAmount в входных данных.

    - Проверяют, что функция выбрасывает исключение, если API ключ не установлен.

    - Проверяют корректность обработки ошибок при запросе к API, включая статус-коды ошибок.

    - Убеждаются, что функция выбрасывает исключение, если API возвращает ответ без ожидаемого поля result.


- **Тесты для `load_transactions`**:
  - Проверяют успешную загрузку данных из корректного JSON-файла.
  - Проверяют обработку ошибок, таких как отсутствие файла или некорректные данные.

### Шаблон для файла `.env.example`

Для тестирования с API создан файл `.env.example` в корневой директории этого проекта,
В этот файл необходимо добавить свой API_KEY.
```
# .env.example
API_KEY=Ваш_api_key_введите_сюда
```


Для работы с API необходимо создать файл `.env` в корневой директории этого проекта. 
Этот файл должен содержать следующие переменные окружения:

```
# .env
API_KEY=Ваш_api_key_введите_сюда
```

Замените `Ваш_api_key_введите_сюда` на ваш реальный API-ключ. 

Чтобы из файла .env "достать" API_KEY:
В модуль с функцией `convert_to_rub`
Загружаем переменные окружения из файла .env
```
    load_dotenv()
```


**Тесты для  `read_financial_operations_from_csv`**:

- `test_file_not_found`: Проверяет, что при отсутствии файла выбрасывается исключение FileNotFoundError.
- `test_invalid_file_format`: Проверяет, что при неверном формате файла выбрасывается исключение InvalidFileFormatError.
- `test_empty_file`: Проверяет, что при попытке чтения пустого файла выбрасывается исключение EmptyFileError.
- `test_successful_read`: Проверяет успешное чтение данных из корректного файла CSV.
- `test_read_file_error`: Проверяет, что при ошибке чтения файла выбрасывается исключение FileReadError.


**Тесты для `read_financial_operations_from_excel`**:
- test_file_not_found: Проверяет, что функция вызывает исключение FileNotFoundError, если указанный файл не существует. 
`Использует мокирование os.path.isfile, чтобы вернуть False.`
- test_invalid_file_format: Проверяет, что функция вызывает исключение InvalidFileFormatError, если файл существует, но имеет неверный формат (например, текстовый файл).
`Мокирует os.path.isfile, чтобы вернуть True.`
- test_read_file_error: Проверяет, что функция вызывает исключение FileReadError, если возникает ошибка при чтении файла.
`Мокирует pandas.read_excel, чтобы вызвать исключение при попытке чтения.`
- test_empty_file: Проверяет, что функция вызывает исключение EmptyFileError, если файл существует, но является пустым (возвращает пустой DataFrame).
`Мокирует pandas.read_excel, чтобы вернуть пустой DataFrame.`
- test_successful_read: Проверяет, что функция корректно читает данные из файла и возвращает их в ожидаемом формате.
`Мокирует pandas.read_excel, чтобы вернуть DataFrame с тестовыми данными, и проверяет соответствие результата ожидаемому.`


**Тесты для `Тестирование функции filter_bank_operations`:**

- Проверка точного совпадения 
- Проверка частичного совпадения 
- Проверка с пустой строкой
- Проверка с несуществующим ключевым словом
- Проверка с неправильным типом transactions
- except ValueError: # Ожидаем исключение
- Проверка с неправильным типом search_str
- Проверка с транзакцией, которая не является словарем

**Тесты для `Тестирование функции count_operations_by_category`:**

- Проверяет, что функция правильно считает количество операций по категориям
- Проверяет, что функция возвращает нулевые значения для всех категорий
- Проверяет, что функция возвращает пустой словарь
- Проверяет, что функция вызывает ValueError, если элемент не является словарем
- Проверяет, что функция правильно считает количество операций, игнорируя регистр


### Логирование.

В проекте реализовано логирование для отслеживания работы функций и обработки ошибок. 
Логирование осуществляется с помощью встроенного модуля `logging` и пользовательского декоратора.
Логгер создаёт папку `logs`, если она не была создана ранее и записывает в неё логи.

#### Настройка логгирования.

Каждый модуль, например, `masks`, имеет свой логгер, который настраивается следующим образом:
```
from .logger import setup_logger
```

▎Настройка логгера для модуля masks
```
logger = setup_logger('masks')
```

#### Пример логирования в функции.

Логирование добавляется в функции для отслеживания входных данных, ошибок и успешных результатов. 
Например, функция `get_mask_account` может выглядеть так:
```
@log("mylog.txt")
def get_mask_account(account_number: Union[str]) -> Union[str, ValueError]:
    """Функция принимает на вход номер счёта и возвращает замаскированный счёт"""
    logger.debug(f'Получен номер счёта: {account_number}')  # Логируем входные данные

    # Проверяем, является ли строка пустой
    if not account_number:
        logger.error("Ошибка: входная строка пустая.")  # Логируем ошибку
        return "Ошибка: входная строка пустая."

    # Проверяем длину номера счёта
    if len(account_number) < 16:
        logger.error("Ошибка: слишком короткий номер счёта.")  # Логируем ошибку
        return "Ошибка: слишком короткий номер счёта."

    elif len(account_number) > 16:
        logger.error("Ошибка: слишком длинный номер счёта.")  # Логируем ошибку
        return "Ошибка: слишком длинный номер счёта."

    # Проверяем, если в номере счёта присутствуют кроме цифр, другие символы
    if not account_number.isdigit():
        logger.error("Ошибка: присутствуют другие символы в счёте.")  # Логируем ошибку
        return "Ошибка: присутствуют другие символы в счёте."

    # Маскируем номер счёта
    masked_account = "**" + account_number[-4:]
    logger.info(f'Замаскированный номер счёта: {account_number}')  # Логируем успешный результат

    # Возвращаем замаскированный номер
    return masked_account
```

**Заключение**
_Этот модуль предоставляет удобные функции для работы с данными, включая маскировку, фильтрацию, сортировку, форматирование,
конвертацию валют используя API, а также предназначены для безопасного и корректного чтения финансовых операций из файлов различных форматов (CSV и Excel)._
_Основная функция программы реализована в функции `main()`:**_

```
  1. *Инициализацию*: Настройка необходимых переменных и ресурсов.
  
  2. *Основной поток выполнения*: Вызов других функций и обработка логики приложения.
  
  3. *Обработка ошибок*: Управление исключениями и вывод сообщений об ошибках.
  
  4. *Завершение работы*: Освобождение ресурсов и завершение программы.
```
Ознакомьтесь с тестами, чтобы понять, как функции работают в различных сценариях._
_В модуле также реализованы декораторы и логгирование, для расширения поведения функций._

***Документация***
Для получения дополнительной информации обратитесь к [документации](README.md).
